package every041;

public class Solution410 {
    public int splitArray(int[] nums, int k) {
        //有多的就有少的
        int max = nums.length - k + 1;
        int avg = (nums.length + k - 1) / k;

        //连续子数组
        for (int i = 0; i < nums.length; i++) {

        }
        return 0;
    }
}
